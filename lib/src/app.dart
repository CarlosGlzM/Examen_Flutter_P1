import 'package:flutter/material.dart';
import 'package:examen_flutter_triage/src/pages/lienso_page.dart';
class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        home: Center(
          child: LiensoPage(),
        ));
  }
}
